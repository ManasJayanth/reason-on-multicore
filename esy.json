{
  "name": "multicore-reason",
  "esy": {
    "buildsInSource": true,
    "buildx": [
      "# ocamlopt -pp 'refmt --print binary' -o #{self.target_dir}/foo -impl #{self.root}/foo.re"
    ],
    "build": "dune build -p #{self.name}"
  },

  "resolutions": {
    "@opam/menhir": "20190924",
    "ocaml": "esy-ocaml/ocaml-multicore#cf70acfa52fda1aacfaef3c1aaa41b544adb4b6f"
  },

  "dependencies": {
    "ocaml": "*",
    "@opam/dune": "*",
    "@esy-ocaml/reason": "facebook/reason:reason.json"
  }
}
